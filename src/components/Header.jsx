export default function Header() {
    // Header is now replaced by Sidebar + TopBar in the dashboard layout
    // This component is kept for backward compatibility but is no longer used
    return null;
}
